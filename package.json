{
  "name": "javascript-and-beers",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export -o dist/",
    "deploy": "git subtree push --prefix dist origin gh-pages",
    "deploy-force": "git push origin `git subtree split --prefix dist master`:gh-pages --force"
  },
  "dependencies": {
    "date-fns": "^2.17.0",
    "gray-matter": "^4.0.2",
    "next": "^12.1.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "remark": "^13.0.0",
    "remark-html": "^13.0.1"
  }
}
